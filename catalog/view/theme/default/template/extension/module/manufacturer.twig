<div class="list-group">
  <a class="list-group-item">{{heading_title}}</a>
  {% for manufacturer in manufacturers %}
  {% if manufacturer.manufacturer_id == manufacturer_id %} 
  <a href="{{ manufacturer.href }}" class="list-group-item active">{{ manufacturer.name }}</a> 
  {% else %} <a href="{{ manufacturer.href }}" class="list-group-item">{{ manufacturer.name }}</a>
  {% endif %}
  {% endfor %}
</div>
